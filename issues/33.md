### Update Annotations for Domains

The annotations have been updated to replace all instances of `mcp.lega4e.dev` with `mcp-auto.ai` as follows:

```yaml
apiVersion: v1
kind: Service
metadata:
  name: petstore-api
  namespace: team-a
  annotations:
    # Required: opt in to MCP proxying
    mcp-auto.ai/enabled: "true"
    
    # Required: tool prefix for this upstream
    mcp-auto.ai/tool-prefix: "pets"
    
    # OpenAPI spec source (one of these)
    mcp-auto.ai/openapi-path: "/openapi.json"          # auto-fetch from service
    mcp-auto.ai/openapi-url: "https://cdn.example.com/specs/petstore.yaml"
    mcp-auto.ai/openapi-configmap: "petstore-spec:openapi.yaml"  # name:key
    
    # Optional: overlay source
    mcp-auto.ai/overlay-configmap: "petstore-overlay:overlay.yaml"
    
    # Optional: outbound auth
    mcp-auto.ai/auth-strategy: "bearer"
    mcp-auto.ai/auth-secret: "petstore-bearer-token"   # Secret name
    
    # Optional: which MCPProxy should pick this up
    mcp-auto.ai/proxy: "main-proxy"
    
    # Optional: port override (default: first port in Service spec)
    mcp-auto.ai/port: "8080"
    
    # Optional: base path prefix
    mcp-auto.ai/base-path: "/api/v2"
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: petstore-api
```

### Update Date
Updated on 2026-04-03 at 15:52:56 UTC.