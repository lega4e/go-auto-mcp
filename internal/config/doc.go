// Package config defines all configuration structs for mcp-auto,
// handles loading from YAML files via koanf, environment variable expansion,
// and hot-reload watching via fsnotify.
package config
