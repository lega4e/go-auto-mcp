// Package config re-exports all configuration types from pkg/config as aliases
// and provides the proxy-specific Loader (koanf YAML loading, env-var expansion,
// and hot-reload watching via fsnotify).
package config
