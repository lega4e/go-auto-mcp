FROM golang:1.25-alpine AS builder

RUN apk add --no-cache ca-certificates

WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /mcp-auto-kong ./cmd/kong

FROM kong:3.9-ubuntu

USER root
RUN mkdir -p /etc/mcp-auto /etc/kong
COPY --from=builder /mcp-auto-kong /usr/local/bin/mcp-auto-kong

ENV KONG_PLUGINS=bundled,mcp-auto
ENV KONG_PLUGINSERVER_NAMES=mcp-auto
ENV KONG_PLUGINSERVER_MCP_ANYTHING_START_CMD=/usr/local/bin/mcp-auto-kong
ENV KONG_PLUGINSERVER_MCP_ANYTHING_QUERY_CMD=/usr/local/bin/mcp-auto-kong --dump
ENV KONG_DATABASE=off
ENV KONG_DECLARATIVE_CONFIG=/etc/kong/kong.yaml
ENV KONG_ADMIN_LISTEN=0.0.0.0:8001
ENV KONG_PROXY_LISTEN=0.0.0.0:8000

EXPOSE 8000 8001
USER kong
