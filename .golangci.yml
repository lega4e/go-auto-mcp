version: "2"
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - misspell
    - revive
    - exhaustive
    - noctx
    - bodyclose
    - contextcheck
  settings:
    revive:
      rules:
        - name: exported
        - name: error-return
        - name: error-strings
formatters:
  enable:
    - gofmt
    - goimports
  settings:
    goimports:
      local-prefixes:
        - github.com/lega4e/mcp-auto
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
